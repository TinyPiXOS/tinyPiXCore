网络模块设计说明：
	NetworkConf和NetworkConflib为旧版接口，对于基础的网络配置使用系统的ioctl相关接口实现，对于应用层相关配置(DNS和DHCP)使用nmcli
	命令行工具来实现。
	NetPcap主要借助于libpcap进行网络抓包来计算网速
	
	nm系列主要为参考NetworkManger的方式使用了DBUS进行网卡管理，当前主要为了替换掉旧版接口的命令行方式。

NetworkManger模块功能：
	NetworkManger：
		网卡以及网络管理

	NmSettings：
		Settings相关接口，可以获取网络配置

	NmDevice：
		Device相关接口，可以理解为网卡硬件相关，

	NmConnection：
		Connection相关接口，可以理解作网络配置，但是此配置不是网卡配置，而是类似于配置文件，可以应用于网卡


最新计划，多管理工具聚合，根据当前环境检测走合适的方式，集成NetworkManager，dhcpd，systemd
	如果有NetworkManager，直接走这个，可以直接实现所有功能(DHCP,DNS)。
	DHCP：NetworkManager --> systemd-networkd --> dhcpcd 
	NDS：NetworkManager --> systemd-resolved --> c-ares  --> libc自带
	WIFI：NetworkManager --> iwd(优先搭配systemd) --> wpa_supplicant(优先搭配dhcpcd)
	热点：NetworkManager --> hostpad
在上层统一封装中间接口层


DNS暂时使用libc自带，后面改为使用c-ares

